# Skillz OpenAI plugin source

This directory contains only the plugin manifest template. The `skills/` payload is generated from the canonical repository skills by `scripts/build_openai_plugin.py` and is intentionally not duplicated here.

Build and installation details: [`../../docs/OPENAI-PLUGIN-DISTRIBUTION.md`](../../docs/OPENAI-PLUGIN-DISTRIBUTION.md).
